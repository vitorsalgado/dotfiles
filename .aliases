alias python=python3

# navigation
alias zshconfig="code ~/.zshrc"
alias zshconfig_local="code ~/.zshrc_local"
alias ohmyzsh="code ~/.oh-my-zsh"
alias workspace="cd ~/workspace"
alias github="cd ~/workspace/github"
alias p="cd ~/workspace"

# nodejs
alias nb="npm run build"
alias nt="npm test"

# k8s
alias k="kubectl"

# general
alias ll="ls -la"
alias ls='ls -F'
alias h="history"
alias g="git"
alias nv="nvim"
alias nvc="nvim ."

# utils
alias listeners='lsof -a +c 0 -i TCP -s TCP:LISTEN'